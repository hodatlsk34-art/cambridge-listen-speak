"use client";
/* eslint-disable @next/next/no-html-link-for-pages */
import { FormEvent, useEffect, useState } from "react";
type User={id:string;login_code:string;display_name:string;level:string;active:number;must_change_password:number;is_minor:number;parent_consent_at:number|null};
export default function AdminPage(){const[users,setUsers]=useState<User[]>([]);const[msg,setMsg]=useState("");const load=()=>fetch("/api/admin/users").then(async r=>{if(r.status===403){location.href="/login";return;}const d=await r.json() as {users:User[]};setUsers(d.users||[])});useEffect(()=>{load()},[]);async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({loginCode:f.get("loginCode"),displayName:f.get("displayName"),password:f.get("password"),level:f.get("level"),isMinor:f.get("isMinor")==="on",parentConsent:f.get("parentConsent")==="on"})});const d=await r.json() as {error?:string};setMsg(r.ok?"Đã tạo tài khoản học viên.":d.error||"Có lỗi xảy ra.");if(r.ok){e.currentTarget.reset();load();}}async function toggle(u:User){await fetch("/api/admin/users",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:u.id,active:!u.active})});load()}return <main className="admin-shell"><header><div><span className="kicker">QUẢN TRỊ HỌC VIÊN</span><h1>Tài khoản được cấp</h1></div><a href="/">← Về trang học</a></header><section className="admin-grid"><form className="admin-form" onSubmit={create}><h2>Tạo tài khoản mới</h2><label>Mã đăng nhập<input name="loginCode" required placeholder="NK-A1-001"/></label><label>Họ tên học viên<input name="displayName" required/></label><label>Mật khẩu tạm<input name="password" type="password" minLength={8} required/></label><label>Cấp độ<select name="level">{["Pre A1","A1","A2 Flyers","A2 Key","B1","B2","C1","C2"].map(x=><option key={x}>{x}</option>)}</select></label><label className="check"><input type="checkbox" name="isMinor" defaultChecked/> Học viên dưới 18 tuổi</label><label className="check"><input type="checkbox" name="parentConsent"/> Đã có xác nhận phụ huynh</label><button className="button primary">Tạo và cấp tài khoản</button>{msg&&<p>{msg}</p>}</form><div className="user-list"><h2>Danh sách ({users.length})</h2>{users.map(u=><article key={u.id}><div><b>{u.display_name}</b><code>{u.login_code}</code><small>{u.level} · {u.is_minor?u.parent_consent_at?"Đã xác nhận phụ huynh":"Chờ xác nhận phụ huynh":"Người lớn"}</small></div><span className={u.active?"status active":"status"}>{u.active?"Đang hoạt động":"Đã khóa"}</span><button onClick={()=>toggle(u)}>{u.active?"Khóa":"Mở khóa"}</button></article>)}</div></section></main>}
