"use client";
/* eslint-disable @next/next/no-html-link-for-pages */
import { FormEvent, useState } from "react";
export default function LoginPage(){const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({loginCode:f.get("loginCode"),password:f.get("password")})});const d=await r.json() as {error?:string;role?:string};setLoading(false);if(!r.ok){setError(d.error||"Không thể đăng nhập.");return;}location.href=d.role==="admin"?"/admin":"/dashboard";}return <main className="account-shell"><form className="account-card" onSubmit={submit}><a href="/" className="account-brand">▥ SpeakUp Cambridge</a><span className="kicker">TÀI KHOẢN HỌC TẬP</span><h1>Đăng nhập</h1><p>Sử dụng mã và mật khẩu do quản trị viên cấp.</p><label>Mã đăng nhập<input name="loginCode" autoComplete="username" required placeholder="Ví dụ: NK-A1-001"/></label><label>Mật khẩu<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<div className="form-error">{error}</div>}<button className="button primary" disabled={loading}>{loading?"Đang kiểm tra...":"Đăng nhập →"}</button><small>Không chia sẻ mã và mật khẩu của bạn cho người khác.</small></form></main>}
